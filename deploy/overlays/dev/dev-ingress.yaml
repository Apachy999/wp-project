apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-ingress
spec:
  rules:
    - host: dev.wp.k8s-5.sa
      http:
        paths:
          - path: /
            backend:
              serviceName: wordpress
              servicePort: 80
